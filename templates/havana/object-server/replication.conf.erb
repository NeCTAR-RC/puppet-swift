[DEFAULT]
bind_ip = 0.0.0.0
bind_port = <%= object_replication_port %>
workers = <%= workers %>
set log_level = WARN
log_level = WARN

[pipeline:main]
pipeline = object-server

[app:object-server]
use = egg:swift#object
replication_server = true

[object-replicator]