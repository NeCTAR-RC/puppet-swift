[DEFAULT]
bind_ip = 0.0.0.0
bind_port = <%= account_replication_port %>
workers = <%= workers %>
set log_level = WARN
log_level = WARN

[pipeline:main]
pipeline = account-server

[app:account-server]
use = egg:swift#account
replication_server = true

[account-replicator]
